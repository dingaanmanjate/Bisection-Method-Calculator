Build the files, then run 
'''sh
./main
'''
from there input the min number in the range
press enter
input the max number in the range

cos(x) -x : [0:1] | exposed a few flaws with the algorithm, when the function decreases it fails. 
potential fix is to tweking the conditional statements. being --[a p b] (their signs)
[--+], [-++], [+--] and [++-]

To use in this point on time, one would have to comment out the other equations from the ones they are trying to test.